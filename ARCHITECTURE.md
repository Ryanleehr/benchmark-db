# 项目架构说明

## 文件清单

- `src/build_dataset.py` - 从上市财报里拉取数据
- `src/test_fetch.py` - 测试取数是否正常
- `data/companies.csv` - 目标公司的目录
- `data/benchmark_raw.csv` - 脚本抓取并合并后的原始数据，8 家公司的营业收入/净利润/职工现金支出

## 调用链
main()
├── fetch_company(code, name)         ← 每家公司调一次，循环里共调 8 次
│   ├── to_ak_symbol(code)             ← 调 1 次
│   ├── fetch_report(symbol, ...)      ← 调 2 次（利润表、现金流量表各一次）
│   └── extract_fields(df, columns)    ← 调 2 次（跟 fetch_report 配对）

## 数据流

fetch_company("603866", "桃李面包") 内部：

1. income：(24, 3) — 报告日、营业收入、净利润
2. cashflow：(24, 2) — 报告日、支付给职工以及为职工支付的现金
3. merge(on="报告日", how="outer") → 按报告日对齐拼接
4. 手动插入 code、name 两列
5. merged：(24, 6)

注意：这次两表报告日完全对齐所以行数不变；如果某期数据缺失，
outer 合并会保留所有日期、缺失处填 NaN，行数可能变化。


## 人工成本率计算逻辑
### 分子分母

* 分子：支付给职工以及为职工支付的现金
* 分母：营业收入

### 报告期口径

* 优先使用1231结尾的行，作为全年数据
* 如果当年这个报告期还没有数据，则用最近报告期的数据计算，并用 data_period 字段标记实际覆盖的period

### 单位与计算

* 分子与分母都是元，计算时可以约掉，结果以百分比呈现。

### 异常处理

* 分子字段缺失（NaN）时：该周期的工资数据及对应的营业收入不做计算，人工成本率结果显示空值。
* 分母为 0 时：算出 `inf`之后，把它当成跟 NaN 一样的"无效值"处理，显示为空，不让 `inf` 流到最终结果里。