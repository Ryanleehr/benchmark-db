services:
  api:
    build: .
    container_name: benchmark
    ports:
      - "8000:8000"
    environment:
      - API_TOKEN=${API_TOKEN}
      - PYTHONPATH=/app/src
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped